---
name: bilibili
version: "1.1.0"
description: 哔哩哔哩 App 测试技能。Use when testing bilibili video playback, search, danmaku, fullscreen, or any UI interaction on tv.danmaku.bili.
app_info:
  package_name: "tv.danmaku.bili"
  launch_activity: ".MainActivityV2"
  login_method: "sms"
  platforms: [android, ios]
triggers:
  - "bilibili"
  - "哔哩哔哩"
  - "B站"
  - "B站App"
  - regex: "tv\\.danmaku\\.bili"
dependencies:
  skills: [app_smoke_test]
  mcp_servers: [appium, vision]
tags: [video, social, entertainment]
sub_skills:
  - video_playback
  - search_flow
---

# 哔哩哔哩 App 测试技能

哔哩哔哩（B站）移动端 App 的自动化测试技能，提供视觉特征识别、UI 交互模式和已知陷阱的完整知识。

## 子技能

| 子技能 | 文件 | 触发关键词 | 说明 |
|--------|------|-----------|------|
| 视频播放 | `video_playback.md` | 视频、播放、全屏、倍速、弹幕 | 播放器控制、全屏、清晰度、倍速、弹幕开关 |
| 搜索功能 | `search_flow.md` | 搜索、search | 搜索入口、输入、结果、边界情况 |

## When to Use

- 测试哔哩哔哩 App 的视频播放功能
- 测试哔哩哔哩 App 的搜索功能
- 测试哔哩哔哩 App 的弹幕、评论、互动功能
- 需要识别 B 站特有 UI 元素（控制栏、弹幕开关、全屏按钮、竖屏按钮等）

## 视觉特征库

### 首页
- **底部导航栏**：4 个 Tab，从左到右依次为「首页」「动态」「消息」「我的」
  - 选中态：图标变为粉色，文字变为粉色
  - 未选中态：灰色图标 + 灰色文字
- **顶部搜索栏**：右侧有放大镜图标，灰色背景圆角矩形
- **推荐流**：上下滑动的视频卡片列表，每个卡片包含封面图 + 标题 + 播放量

### 搜索页
- **搜索框**：顶部白色输入框，内有灰色 placeholder「搜索」
- **搜索历史**：输入框下方展示历史搜索词标签（圆角灰色标签）
- **搜索建议**：输入时下方实时出现建议列表
- **搜索结果**：视频卡片列表，左侧封面图 + 右侧标题/UP主/播放量

### 播放页
- **视频区域**：占据屏幕上半部分，竖屏模式
- **UP 主信息**：视频下方显示头像 + 昵称 + 关注按钮
- **互动区**：右侧竖排「点赞」「投币」「收藏」「分享」图标
- **推荐列表**：下方相关视频卡片列表
- **控制栏（默认隐藏）**：点击视频区域后浮现，3~5 秒后自动消失
  - 底部：进度条 + 播放/暂停按钮 + 全屏按钮（最右侧）+ 竖屏
  - 顶部：返回按钮 + 视频标题
- **弹幕层**：视频上方滚动的文字流（可关闭）

## 常见弹窗处理

| 弹窗 | 视觉特征 | 处理方式 |
|------|---------|---------|
| 青少年模式 | 白色弹窗，标题「青少年模式」，底部蓝色按钮「我知道了」 | 点击「我知道了」 |
| 版本更新 | 白色弹窗，标题「发现新版本」，右下角灰色文字「以后再说」 | 点击「以后再说」 |
| 通知权限 | 系统级弹窗，包含「允许」「拒绝」 | 点击「拒绝」 |

## 已知陷阱

- **搜索历史弹窗**会遮挡搜索框（需先关闭）
- **软键盘弹出**后搜索按钮可能被遮挡
- **弱网下搜索**会超时并显示"网络不给力"
- **控制栏默认隐藏**：播放页看不到播放/暂停/全屏按钮/竖屏，必须先点击视频区域
- **控制栏自动消失**：浮现后几秒无操作即隐藏，操作需在浮现期间完成
- **竖屏不在首页**：必须先进入播放页再触发控制栏
- **互动区不在首页**：点赞、投币、收藏、分享按钮在视频播放页右侧，首页推荐流没有这些按钮
- **首页底部 Tab 容易误点**：底部导航栏与页面内容区接近，坐标定位时注意区分

## Tips

- B 站的视频播放控制栏是隐藏的，所有控制栏操作都需要先 `tap_first` 唤出控制栏
- 弹幕开关是常驻显示的，不需要 tap_first
- 双击视频区域可以快速暂停/恢复播放，比通过控制栏更可靠
- 全屏模式下屏幕方向变为横屏，所有坐标系都会变化
- 清晰度切换必须在全屏模式下才能操作
